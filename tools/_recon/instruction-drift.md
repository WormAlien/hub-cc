# Instruction drift: `ADDING-A-GATEWAY.md` vs живой код

Источник инструкции: `D:\WORMALIENAIGIGANT\wiki\abuse-hub\ADDING-A-GATEWAY.md`
Код: `C:\Users\WormAlien\Desktop\Autoreger_Clean`
Дата сверки: 2026-09-13. Только чтение, ничего не правилось.

Размеры на момент сверки: `routing/transparent-proxy.js` — 24 945 строк,
`routing/proxy-dashboard.html` — 34 109 строк (инструкция говорит 20 000+/25 000+).

Уже известные четыре расхождения (перепроверены, подтверждены): №1 `pools`, №3 keepalive-порт,
№4 `ref-codes.default.json` по полному имени, №5 имена фронтовых лоадеров.
Ниже — остальные найденные.

---

## A. Расхождения, найденные в этой сессии

| # | Что говорит инструкция | Что в коде | Файл:строка | Чем грозит |
|---|---|---|---|---|
| A1 | `newapiLkBusy`: массив `pools` = 9 pid-карт, шаблон `[arLkPids, goLkPids, tbLkPids, jwLkPids, kkLkPids, xxLkPids, hnLkPids, skLkPids, tsLkPids, xpLkPids]` | 12 карт: `[arLkPids, goLkPids, tbLkPids, jwLkPids, kkLkPids, apLkPids, hnLkPids, akLkPids, rmLkPids, skLkPids, tsLkPids, xpLkPids]` — нет `xxLkPids` места, зато есть три шлюза, которых в инструкции нет: `apLkPids` (AIPM), `akLkPids` (aikeysapi), `rmLkPids` (rumeng) | `transparent-proxy.js:8394` | Молчаливая поломка: если копировать шаблон инструкции дословно, из массива выпадут `ap/ak/rm` — у трёх живых шлюзов отвалится детект открытого браузера (грабля #21 в полном объёме). Инструкция знает про 9 шлюзов, в коде 12 |
| A2 | §1.2 даёт `makeKeepaliveHandlers(Number(process.env.XX_KEEPALIVE_PORT || PORT))` — форма с env + fallback | У KKtoken fallback — **литерал**: `makeKeepaliveHandlers(Number(process.env.KK_KEEPALIVE_PORT || 20161))`. Форма `\|\| PORT` есть только у части шлюзов | `transparent-proxy.js:1485` (KK), ср. у AIPM/др. | Не поломка, а неточность: копируя дословно, получишь рабочий код, но `\|| PORT` вместо `\|| 20161` — резерв другой. Хуже обратное: если новый гейтвей скопирует `\|| 20161` у KK, при отсутствии env оба процесса полезут на один порт |
| A3 | Реестр `BACKENDS` перечислен как «одна точка» (§1.8a) | Есть **второй** реестр коротких префиксов, инструкция о нём не знает: `agentrouter: 'ar', gorouter: 'go', kktoken: 'kk', aipm: 'ap', hcnsec: 'hn', ...` | `transparent-proxy.js:873` | Молчаливая поломка: без записи в этом объекте новый шлюз получает `undefined`-префикс в местах, где префикс берётся по полному имени |
| A4 | §1.8 не упоминает `ghLkPidsByTag` | Живой реестр-сосед: комментарий прямо требует «заселять сюда готовую карту» вместе с GH_POOL_* | `transparent-proxy.js:15287` | Молчаливая поломка: новый шлюз не попадает в детект занятости GitHub-аккаунтов по pid |
| A5 | §1.8a `BACKENDS`: ключ — полное имя, `~строка 187` | Реально строка 199; в объекте уже есть `aipm` (шлюз, добавленный после написания инструкции) | `transparent-proxy.js:199`, `412` | Неточность (номера строк) |
| A6 | §0.1: «Следующий свободный: **20163**» | 20163 **занят AIPM** (`AP_KEEPALIVE_PORT = 20163`), вкладка и бэкенд живут | `transparent-proxy.js:13982`, `:14184` | Молчаливая поломка при слепом следовании: инструкция выдаёт занятый порт как свободный. Порт нужно искать самому по `lifecycle.js`/константам, а не по списку §0.1 |
| A7 | §1.1: «Найти блок KK-констант — от `const KK_SESSIONS_FILE` до закрывающей `};` объекта `KK_CC_HEADERS`» | Верно по составу (12 констант + `KK_CC_HEADERS` совпадают 1:1), но `~12612–12644` → реально 13931–13976 | `transparent-proxy.js:13931-13976` | Неточность (номера строк) |

---

## B. Frontend — `routing/proxy-dashboard.html`

| # | Что говорит инструкция | Что в коде | Файл:строка | Чем грозит |
|---|---|---|---|---|
| B1 | §2.2: «Найти `<!-- ═════════ TAB: KKTOKEN ══════════ -->` и следующий `<!-- ═════════ TAB:` — это граница» | Маркер `TAB: KKTOKEN` встречается **ДВАЖДЫ**: 6923 (настоящий KK, `data-tab-content="kktoken"`) и **7082 — над панелью AIPM** (`data-tab-content="aipm"`). Второй маркер — копипастная ошибка, панель AIPM осталась подписанной «KKTOKEN» | `proxy-dashboard.html:6923`, `:7082`, `:7083` | Молчаливая поломка: `grep 'TAB: KKTOKEN'` даёт два хита, и по второму клонируется панель AIPM вместо панели KKtoken (другие id-префиксы `ap-*`, другой хост, другой порт, другой реф-код). Плюс сама инструкция противоречит себе: границы «KK-блока JS» она потом ищет по «TABI TOKEN разделителю», а не по этому правилу |
| B2 | §2.1 поиск: `grep -n 'data-tab="kktoken".*nav-btn'` | Порядок атрибутов обратный — `class="nav-btn …"` стоит ДО `data-tab="kktoken"` (как и в шаблоне самой инструкции) | `proxy-dashboard.html:668` | Неточность с последствием: поиск из инструкции возвращает **ноль строк**. Агент делает вывод «кнопки нет» и идёт искать дальше/клонирует не то. Правильный запрос — `grep -n 'data-tab="kktoken"'` |
| B3 | §2.5: `xxLoadSessions(false)` | Функция зовётся `loadKkSessions` (и `loadKkSessionsLight`) — префикс `load` + `Kk`, а НЕ `kk` + `Load`. Так же у всех новых шлюзов: `loadApSessions`, `loadHnSessions`, `loadAkSessions`, `loadRmSessions` | `proxy-dashboard.html:9033` (kktoken), `:15844`, `:15863`; ср. `:16380`, `:16920`, `:17633`, `:18293` | Молчаливая поломка: `xxLoadSessions` — та же несуществующая функция, `ReferenceError` при открытии вкладки. Инструкция здесь противоречит сама себе: механическое правило §2.14 («заменить `kk` → `xx`») даёт ПРАВИЛЬНОЕ `loadXxSessions`, а явный шаблон §2.5 — нет |
| B4 | §2.10: `reload: () => xxLoadSessionsLight()` | `reload: () => loadKkSessionsLight()` | `proxy-dashboard.html:24592` | То же, что B3 — `ReferenceError` в `MONEY_PROVIDERS`. Это и есть известное расхождение №2, здесь подтверждено с точной строкой |
| B5 | §2.7 — три обёртки подряд «load + apply + reset» | Живые обёртки: `kkLoad`/`kkApply` — 13394–13395, а `kkReset` — **13413**, через 17 строк, отдельной пачкой `*ResetKeepalive`. Более новые шлюзы ещё дальше: `apLoad`/`apApply` 13414–13415, `apReset` 13416, `ts`/`hn`/`ak`/`rm` reset — 13424–13430 | `proxy-dashboard.html:13394-13430` | Неточность: вставляя «три строки подряд» после `kkLoadKeepalive`, получишь reset в неверном месте — не поломка (соседний блок тоже валиден), но структура файла поедет от образца |
| B6 | §2.2: блок «от `<!-- TAB: KKTOKEN -->` до следующего `<!-- TAB:`» | Панель KK действительно там (6923→7082), **но** инструкция не упоминает `data-ref-link="kktoken"` в шапке панели как обязательную замену — только в грабле #13. В живой панели AIPM атрибут уже `data-ref-link="aipm"` | `proxy-dashboard.html:6924`, `:7090` | Неточность (грабля есть, но не в основном списке замен) |
| B7 | §2.11 `DEFAULT_TABS_VISIBLE`, §2.12 `NAV_COUNT_JOBS` | Реальные строки: 28138 и 28524. В обоих массивах уже присутствуют `aipm`, `hcnsec`, `aikeysapi`, `rumeng` — то есть каждый новый шлюз добавляется в ОБА списка, и это не только «две точки из таблицы» | `proxy-dashboard.html:28138`, `:28524` | Неточность (номера строк устарели: инструкция даёт ~12900/19000-й диапазон) |
| B8 | §2.3/2.4: `LABELS` и `COLORS` — «рядом с LABELS» | Оба объекта разбиты на ДВЕ физические строки (9106–9107 и 9108–9109). Ключ `kktoken` — в конце первой строки, `aipm/hcnsec/aikeysapi/rumeng/xpeach` — во второй | `proxy-dashboard.html:9106-9109` | Неточность: правка «допиши перед `}`» попадёт в конец второй строки — формально верно, но `grep -n "kktoken:.*'KKtoken'"` показывает только первую половину объекта |

---

## C. Бэкенд — реестры и роуты

| # | Что говорит инструкция | Что в коде | Файл:строка | Чем грозит |
|---|---|---|---|---|
| C1 | §1.8 «Реестры (9 точек)» | Реестров, куда обязан попасть новый шлюз, **не 9, а 12**: к девяти из инструкции добавляются `ROUTE_EP`, `ghLkPidsByTag` и `BACKEND_ALIASES` | см. C2–C4 | Молчаливая поломка: три реестра просто не будут заполнены |
| C2 | §1.8 не знает о `BACKEND_ALIASES` | `const BACKEND_ALIASES = { ar: 'agentrouter', …, kk: 'kktoken', hn: 'hcnsec', ap: 'aipm', ak: 'aikeysapi', rm: 'rumeng', … }` — карта «короткий код в `/model` → полное имя бэкенда» для front-door | `transparent-proxy.js:696` | Молчаливая поломка: без записи окно CC **не сможет адресовать шлюз префиксом** (`/model aipm/claude-opus-4-6[1m]`) — фронтдор просто не найдёт бэкенд. Это ровно тот механизм, ради которого две сессии сидят на разных шлюзах. В инструкции его нет вообще |
| C3 | §1.8j: «Добавить `xxLkPids`» — и всё про `newapiLkBusy` | Есть **вторая** карта pid-ов с ЛИТЕРАЛЬНЫМ списком: `ghLkPidsByTag()` → `{ github: ghLkPids, ar:…, go:…, tb:…, xp:…, jw:…, sk:…, ts:…, kk: kkLkPids }`. В ней нет `ap`, `ak`, `rm`, `hn` | `transparent-proxy.js:6408` | Неточность/поломка: инструкция предупреждает про `pools`, но умалчивает о второй карте. Для нового шлюза с GitHub-входом её тоже нужно дополнить, иначе harvester заберёт профиль с живым браузером |
| C4 | §1.8 — «реестры» перечислены как плоский список из 9 | `ROUTE_EP` (префикс эндпоинтов `/__switch/api/<ep>/…`) — отдельный реестр, **не равный** `CC_MODEL_PREFIX`; это прямо оговорено комментарием в коде | `transparent-proxy.js:871-878` | Молчаливая поломка: перепутать `ROUTE_EP` и `CC_MODEL_PREFIX` — роуты уедут на чужой префикс |
| C5 | §1.8c–f: «**Четыре** однострочных объекта GH_POOL_*» — заполнять всем | Живой состав: `GH_POOL_*` содержит 9 тегов (ar, go, tb, xp, jw, sk, ts, kk, ap), но **`hn` в них нет намеренно** — у HCNsec GitHub-вход отключён (`github_oauth=false`), и комментарий предупреждает «заселять сюда готовую GitHub-сессию физически некуда» | `transparent-proxy.js:6497-6502`, `:15284-15288` | Неточность с последствием: инструкция требует заполнить все четыре реестра безусловно. Для шлюза без GitHub-входа это либо мусорная запись, либо пустая папка `gh-sessions/` в `.gitignore` (у hcnsec и aikeysapi её сознательно нет) |
| C6 | §1.9 «Keepalive роуты (3 штуки, **отдельный блок** ~строки 19051–19067)» | Три роута KK **не подряд**: `state` — 23631, `config` — 23633, `latency` — 23653. Между ними — роуты ещё 11 шлюзов; пары `state`/`config` чередуются (`kk state`, `ap state`, `kk config`, `ap config`, `hn state`, `hn config`, …), а все `latency` вынесены в отдельную пачку ниже | `transparent-proxy.js:23631`, `:23633`, `:23653` | Неточность с реальным риском: агент ищет «отдельный блок» и не находит; копируя «три строки подряд» встык, вставит их не по образцу |
| C7 | §1.9 «GitHub-роуты (3 штуки…)», но перечислено **ДВА** (`set-github`, `add-github`) | Роутов ровно **два**: `set-github` — 23574, `add-github` — 23964 (разнесены на 390 строк). Третий кандидат, `map-profiles`, инструкция сама же включает в «основные 18». Итог 18 + 3 + 2 = 23 — совпадает с `grep -c` по коду | `transparent-proxy.js:23574`, `:23964` | Неточность: число «3» сбивает счёт, агент ищет несуществующий третий роут |
| C8 | §1.9 «Основные роуты (18 штук)» — верно по количеству | В коде 18 строк, но они лежат на **23699–23716**, а не «19093–19111» | `transparent-proxy.js:23699-23716` | Неточность (номера строк — файл вырос на ~4 800 строк) |
| C9 | §5 грабля #16: «`normalizeCcModel()` … дописывает суффикс `[1m]` к `claude-opus-*`, `claude-sonnet-*` и `glm-5.3`. **GPT-модели суффикс не получают**» | Регексп: `/^(?:[A-Za-z0-9_.-]+\/)?(claude-(opus|sonnet)-|glm-5\.3(?:$|-)|gpt-5\.6-(?:sol|luna|terra)$)/` — **GPT-модели `gpt-5.6-sol/luna/terra` суффикс получают**. Инструкция утверждает обратное | `transparent-proxy.js:373` | Прямое противоречие: по инструкции агент НЕ ставит `[1m]` GPT-модели и считает это нормой, по коду она ставится — расхождение в ожидаемом содержимом `settings.json`, окно 200k вместо 1M при ручной отладке |
| C10 | §5 грабля #16: «Никаких ручных действий не нужно» | Плюс к регекспу есть вторая ветка (правка 12.09): **голое имя шлюза** из `CC_MODEL_PREFIX` тоже получает `[1m]`, и заведён extension-point `NO_1M_GATEWAYS` (сейчас пустой — в комментарии сказано, что AIKeysAPI и rumeng используют тот же маркер, что и все) | `transparent-proxy.js:336-340`, `:372` | Неточность: инструкция описывает поведение до 12.09, ветку «голое имя шлюза» и `NO_1M_GATEWAYS` не знает |
| C11 | §7.2: «Добавлено в **9 провайдеров**: AR, GoRouter, KKtoken, AIPM, HCNsec, JustWoker, SeekAi, Tabi, XPeach» | Ровно 9 вызовов `if (target.ghId) ghReviveIfNeeded(target.ghId);` — список совпадает 1:1. Но в файле уже **12** New-API-шлюзов; у `AIKeysAPI` (`handleAkActivate`) и `rumeng` (`handleRmActivate`) вызова нет | `transparent-proxy.js:13051, 13814, 14657, 15176, 15880, 18934, 19589, 21116, 21737` | Неточность: инструкция подаёт «9» как полный охват. На деле охват = «шлюзы с GitHub-входом», и это условие в тексте не сформулировано |
| C12 | §6.4: добавить конвертер в `SERVICES` с полями `script` и `provider` | Эталонный AR-конвертер (:20132) живёт **не в `SERVICES`, а в `children()`**, и у него из полей только `{ port, name, respawn: true }` — ни `script`, ни `provider` | `routing/lifecycle.js:140` против `:112-119` | Неточность/ловушка: агент, копирующий «эталон из §6», переносит в `SERVICES` запись без `script`/`provider` — `applyTarget` её не поднимет. В самом файле комментарий объясняет, почему три конвертера остались в старом списке |
| C13 | §3.2: `$perPort = @{…}`, шаблон `KEY_FILE = "$profileDir\.claude\PREFIX-active-key.txt"` — **с бэкслэшем** | Инструкция права, а **код сломан**: у AIPM `KEY_FILE = "$profileDir.claude\aipm-active-key.txt"` — бэкслэш потерян, путь указывает на `C:\Users\WormAlien.claude\…`. В том же блоке UPSTREAM `https://emtf.aipm9527.xyz`, а рабочий хост — `.online` | `routing/keepalive-restart.ps1:65-66` | Обратный дрейф: правильный шаблон — в инструкции, а в коде-образце («последняя запись») два дефекта. Копируя последнюю запись `$perPort`, агент унаследует оба. Ручной рестарт AIPM через этот скрипт сейчас либо берёт ключ не оттуда, либо бьёт в несуществующий хост |
| C14 | §3.2: «обновить сообщение об ошибке (`Write-Error "Unknown port..."`) — добавить новый порт в список» | Список в `Write-Error` заканчивается на 20165 (порт AIKeysAPI там есть), но **шапка-комментарий файла — нет**: `# Instances: … 20162 = HCNsec, 20163 = AIPM, 20164 = WisdomSatan.` — 20165 отсутствует | `routing/keepalive-restart.ps1:4-6` против `:75-76` | Неточность: инструкция помнит про `Write-Error`, но не про шапку. Второй список портов остаётся неполным |
| C15 | §3.7 `internal/hub-balance.js`: «Найти `POOLS`. Добавить `{ id: 'xx', file: 'PREFIX-sessions.json', name: 'NAME' }`» | Форма записи верна, но инструкция не говорит **условия попадания**: `POOLS` — это только «Живые денежные шлюзы», XPeach исключён намеренно (легаси), а `rumeng`/`seekai`/`truesota` там нет вовсе; комментарий отдельно предупреждает про `balance` (доллары) против `balanceLocal` (юани, ×7.3) | `internal/hub-balance.js:22-41` | Неточность с денежным последствием: юаневая цифра, сложенная как долларовая, даёт ошибку ×7.3 в «Общем запасе» |
| C16 | §3.6: «`HOST_AUTH` — добавить `'HOST': 'TBD'`. Определить тип авторизации (обязательно до первого чека баланса)» | В коде логика мягче и **асимметрична**: `const kindEff = kind !== 'jwt' && /(?:^\|;\s*)new_api_refresh=/.test(cookie) ? 'jwt' : kind;` — таблица только ПОВЫШАЕТ classic→jwt по содержимому профиля, но **не понижает jwt→classic** | `routing/lib/newapi-account.js:1275` | Неточность в обе стороны: (а) комментарий рядом (`:231-234`) утверждает «Ошибка в эту сторону НЕ громкая: путь выбирается по содержимому профиля» — это верно только для classic-ветки; ошибочный `jwt` НЕ самопочинится, и грабля #24 инструкции (KKtoken, AIPM) это подтверждает. (б) Наоборот, для classic-панели ошибку исправлять не обязательно — код вытянет сам |
| C17 | §1.8g `NEWAPI_PROFILE_DIRS` — «добавить строку после `'kktoken.cc'`» | Список вырос до 12 хостов (есть `api.rumeng-ai.com`), при этом живой шлюз **`api.wisdomsatan.club` в нём отсутствует** — хотя он есть в `HOST_AUTH`, в `POOLS` (hub-balance) и в `lifecycle.js` | `transparent-proxy.js:8239-8260` | Поломка в самом коде (не дрейф инструкции): у wisdomsatan нет маппинга профиля → точный баланс по кукам профиля для него недостижим. Инструкция §1.8g при этом формально права |
| C18 | §1.8a: «Ключ — полное имя… Вставить после закрывающей `},` блока KKtoken» | В `BACKENDS` у KKtoken нет `provider`-поля; запись заканчивается комментарием и `},`. Форма из §1.8a совпадает | `transparent-proxy.js:199-211` | Неточность (только номер строки: 199, не ~187) |
| C19 | §1.8b: `CC_MODEL_PREFIX` — «Ключ — полное имя, значение — полное имя» | Верно, но в объекте есть **дублирующий ключ** `ak: 'aikeysapi'` рядом с `aikeysapi: 'aikeysapi'` — одна и та же сущность заведена дважды | `transparent-proxy.js:412`, `:416` | Неточность: инструкция подаёт объект как строго «полное имя → полное имя»; при копировании образца можно завести лишний синоним |
| C20 | §1.8h `MONEY_GW`: «Ключ — short prefix» | Совпадает по форме; реально строка 22058, а не ~17854 | `transparent-proxy.js:22058` | Неточность (номера строк) |
| C21 | §1.8i `keepaliveInstances()`: «Вставить после `[KK_KEEPALIVE_PORT]:`» | Запись KK — 20652, но список идёт не в порядке портов и **заканчивается `frontdoorPort()`** — ниже лежат ap/hn/ak/rm и Front Door с полем `statusPath` | `transparent-proxy.js:20643-20661` | Неточность: вставка «после KK» формально валидна, но ломает сложившийся порядок (по группам шлюзов, Front Door последним) |
| C22 | §5 грабля #19 и #22 — про `kkPidAlive` | На момент правки `:8393` комментарий фиксирует, что **AIPM зовёт `kkPidAlive`** («функции нет (aipm зовёт kkPidAlive), и разнобой имён здесь уже стоил детекта»), а `:14831` — что AIPM всё-таки завёл свой предикат. То есть грабля отработала и оставила в файле оба состояния | `transparent-proxy.js:8393`, `:14318`, `:14831` | Неточность: инструкция описывает граблю как «предупреждение на будущее», в коде она уже была наступлена |

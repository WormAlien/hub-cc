# Node modules
node_modules/

# Логи и данные
logs/
output/
accounts.txt
.quota_cache.json
known_bins.json
timing_profile.json
_log_hits.txt
_nul

# Сессии и аккаунты (приватные данные)
manual_sessions/
errors/
ready_to_sell/

# FreeModel приватные данные (cookies, localStorage, аккаунты, ключи, логи)
freemodel/imports/
freemodel/accounts/
freemodel/sessions/
freemodel/keys.txt
freemodel/tg_pool.json

# FreeModel email-backend: IMAP-креды и личные домены. .env.example IS committed.
freemodel/.env
freemodel/.email_domain
freemodel/.email_backend

# VyceAI приватные данные (ключи API)
vyceai/keys.txt
vyceai/keys.json
freemodel/tg_pool.json.bak-*
freemodel/.tg_health_cache.json
freemodel/.email_blocklist.json
freemodel/manual_email.txt
freemodel/lib/camoufox_tmailor_profile_*/
freemodel/lib/probe_profile*/
freemodel/lib/camoufox_recordings/
freemodel/logs/
freemodel/.last_invite

# TG-сессии из архивов магазина: .session = живой auth_key, .json = сайдкар с
# app_id/app_hash. Плюс отчёты и одноразовые скрипты разбора — в них ключи
# лежат открытым текстом.
freemodel/_import_sessions/
freemodel/_import_*.js
freemodel/_import_*.json
freemodel/_recheck_*.js
freemodel/_recheck_*.json
freemodel/_check_one.js
freemodel/_do_import.js
freemodel/_sample.js

# Conduit приватные данные (сессии, sk-cdt- ключи, профили браузера, дампы).
# Коммитим только КОД (conduit/*.js, conduit/lib/*.js).
conduit/accounts/
conduit/logs/
conduit/.tg_used.json
conduit/.last_ref
freemodel/emailnator_debug_*.png
freemodel/inbox_debug_*.png
freemodel/probe_email_out/
freemodel/probe_shots/
freemodel/probe_*.js

# Notion приватные данные (сессии, скриншоты, логи, конфиг с картой)
notion/sessions/
notion/ready/
notion/errors/
notion/logs/
notion/config.js
notion/notion_session_*.json
notion/notion_payment_*.png
notion/notion_after_card_*.png
notion/error_*.png
notion/emailnator_*.png
notion/.dashboard-config.json
notion/.dashboard-cache.json

# Скриншоты отладки
*.png
*.jpg
*.jpeg
# README-скриншоты — трекаем
!docs/*.png

# Битые артефакты
nul
C\357*

# Claude Code / Obsidian локальные настройки
.claude/
.obsidian/

# Временные сессии
devin_session_*.json

# Бэкапы
backup_*/
backups/
*.backup

# Документы продаж
SALES_*.md
CONTEXT.md

# Временная документация
CURRENT_CONFIG.md
DOCUMENTATION_INDEX.md
PROJECT_INFO.md
QUICK_START.md
SESSIONS_STATUS.md

# Батники: локальные не пушим, точки входа и routing/ — пушим.
# 🪤 Здесь allowlist, а не blocklist: новый .bat в корне по умолчанию НЕ едет в репо.
# Так и потерялся SHARE.bat (в README описан, в git его нет вообще), а HUB.bat едва
# не уехал тем же путём — а на него показывают ВСЕ форвардеры, то есть на свежем
# клоне под Windows не запускалось бы ничего. Добавил файл в корень — добавь строку.
#
# UPDATE.bat / DOCTOR.bat / SHARE.bat / FIX.bat сняты 24.08 при уборке корня (было
# 30 файлов): обновление, диагностика и «поделиться» стали пунктами меню хаба.
*.bat
!routing/*.bat
!HUB.bat
!START.bat

# Routing dir runtime artefacts
# Пул прокси: конфиг с кредами, привязки «аккаунт → адрес» и разовые seed-файлы для
# дочернего open-session.js. Ни одно из этого не коммитится: в конфиге логин/пароль
# прокси, привязки — состояние конкретной машины, seed живёт один запуск.
routing/proxy-pool.json
routing/proxy-assign.json
routing/runtime/
routing/*.log
routing/*.err
routing/*.log.err
routing/*.log.out
routing/mode.txt
routing/keepalive-config-*.json
# Архив настроек предыдущей версии дефолтов (пишется при смене CFG_VERSION)
routing/keepalive-config-*.json.v*.bak
# Суточная история времени ответа (минутные бакеты, у каждого своя)
routing/keepalive-latency-*.json
# История событий прокси: те же минутные бакеты, что латентность — данные, не конфиг
routing/keepalive-events-*.json
routing/keepalive-events-*.json.tmp
# Личность установки (installId + ник) — создаётся на машине самим дашбордом
# (transparent-proxy.js), в публичный репозиторий ей нельзя
routing/hub-identity.json
# Архивы логов прокси: имя со временем, поэтому под `routing/*.log` не попадают
routing/*.log.*
# Журнал настоящих токенов (usage-tap во фронт-дворе) — локальные данные расхода
routing/token-usage.jsonl
# Отметки «когда открывали ЛК аккаунта» — локальное состояние стража балансового кеша
routing/newapi-lk-opened.json
# Логи вложенных модулей (в tr_debug.log лежал абсолютный путь прежней папки репо)
routing/**/*.log

# Routing secrets (real keys). .env.example IS committed.
routing/.env

# Telegram bot secrets (token). tgbot/.env.example IS committed.
tgbot/.env
tgbot/bot.log

# Aerolink ручной пул ключей (содержит реальные aero_live_*). al-sessions.example.json IS committed.
routing/al-sessions.json

# Evomap ручной пул ключей (sk-evomap-*). evomap-sessions.example.json IS committed.
routing/evomap-sessions.json

# Ourtoken ручной пул ключей (usTHAz8-*). ourtoken-sessions.example.json IS committed.
routing/ourtoken-sessions.json

# Cun.ai ручной пул ключей (sk-*). cun-sessions.example.json IS committed.
routing/cun-sessions.json

# AgentRouter ручной пул ключей (sk-*). AgentRouter-sessions.example.json — при необходимости.
routing/agentrouter-sessions.json

# Cun.ai автореги: аккаунты, отладочные записи со скриншотами, логи.
cun/accounts/
cun/recordings/
cun/recording_current/
cun/logs/

# Video API ключи (fal/Replicate/Veo/Kling/…). video-keys.example.json IS committed.
routing/video-keys.json

# Video trial-сайты — пользовательские статусы (working/dead). Seed-список в коде.
routing/video-trials.json

# Image API ключи (NanoBanana/fal/Replicate/Imagen/…). image-keys.example.json IS committed.
routing/image-keys.json

# Image trial-сайты — пользовательские статусы (working/dead). Seed-список в коде.
routing/image-trials.json

# GitHub-аккаунты (купленные: логин/пароль/2FA-секрет/recovery). github-accounts.example.json IS committed.
routing/github-accounts.json

# Операционные системы
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# npm
package-lock.json

# Дублирующие данные
autoreger_data/

# Playwright MCP runtime artefacts
.playwright-mcp/

# Archived subprojects (moved out of working tree but kept locally)
# viktor-bridge-main/
# notion-abuz_ai/

# Root configs with secrets
/config.js
# freemodel/config.js — нужен прокси на старте (require '../config'), секретов нет → в репо

# Python environments
.venv/
__pycache__/
*.pyc

# TokenRouter runtime/sensitive data
routing/tokenrouter/chrome-profile/
routing/tokenrouter/camoufox-*/
routing/tokenrouter/sessions/
routing/tokenrouter/debug/
routing/tokenrouter/accounts.json
routing/tokenrouter/*.png
routing/tokenrouter/*.log

# Recorder output
recordings/

# Telegram session tooling — heavy binaries + LIVE tdata (secrets), never commit.
# Keep only the code: tools/tg-open.py + tools/tg-venv-requirements.txt
tools/tg-venv/
tools/telegram-portable/
tools/tg-profiles/

# Grok сессии (живые куки + артефакты device-auth). Код (launcher.py и пр.) коммитим.
grok-launcher/cookies/
grok-launcher/xai_cookies.json
grok-launcher/xai_after_grok.json
grok-launcher/xai_device_auth.log
grok-launcher/*_text.txt
grok-launcher/probe*.out
grok-launcher/probe_out.txt
grok-launcher/sso_probe*.py

# Автодампы схем MCP-тулов (генерируются инструментом, не наш код).
mcps/
doctor-report.txt

# ── Custom providers (дашборд) ─────────────────────────────────────────────
# Реальные ключи провайдеров вкладки Custom (BluesMinds, GoRouter, …) — НИКОГДА
# не коммитить. Формат — custom-providers.json.example. Кеш моделей — runtime.
routing/custom-providers.json
routing/custom-models-cache.json

# ── opencode: локальный конфиг с реальным ключом провайдера claude-popusk ─
opencode.json

# ── Профили Camoufox/FireFox для проб (cookies, logins, кеш) ───────────────
*probe*_prof/
freemodel/lib/camoufox_emailnator_profile_*/

# ── Автореги: аккаунты и живые сессии ───────────────────────────────────────
helpcoder/accounts/
svrtr/accounts/
svrtr/.tg_used.json
anymodel/accounts/
anymodel/.tg_used.json
anymodel/logs/
anymodel/lib/camoufox_anymodel_profile_*/

# ── Отладочные дампы проб ───────────────────────────────────────────────────
out_hc*
probe*.txt
tm_captcha_modal.html

# ── Postman: локальная конфигурация workspace/globals ──────────────────────
postman/
.postman/

# Debug garbage quarantine (swept 2026-08-13, restore via MANIFEST.txt)
_debug_garbage_*/

# ── SeekAi (вкладка дашборда): приватные данные ───────────────────────────────
# Провайдер добавлен 25.08, и его папка приехала в дерево нетрекаемой: 726 файлов,
# среди них профили браузера и ЖИВЫЕ куки GitHub (seekai/gh-sessions/acct_*.json).
# Репозиторий публичный — `git add -A` без этих строк опубликовал бы их. Код
# (open-session.js) и modelmap коммитим, всё остальное — никогда.
seekai/profiles/
seekai/sessions/
seekai/gh-sessions/
routing/seekai-sessions.json

# ── GoRouter (вкладка дашборда): приватные данные ─────────────────────────────
# Профили браузера (куки GitHub/gorouter, логины, кеш) и живые сессии — НИКОГДА
# не коммитить. gorouter-modelmap.json — безопасный конфиг маппинга, коммитим.
gorouter/profiles/
gorouter/sessions/
gorouter/gh-sessions/
routing/gorouter-sessions.json

# ── AgentRouter (вкладка дашборда) ───────────────────────────────────────────
# Сессии storageState (старая схема, не используется с 2026-08-13).
agentrouter/sessions/

# Профили браузера AgentRouter (куки GitHub/agentrouter, логины, кеш) — НИКОГДА
# не коммитить. open-session.js держит профиль на диск на аккаунт.
agentrouter/profiles/

# Резервные копии GitHub-сессий профилей (снимаются перед чек-ин-разлогином, чтобы
# сессию можно было вернуть). Живые куки — НИКОГДА не коммитить.
agentrouter/gh-sessions/

# ── Tabi Token (вкладка дашборда): приватные данные ──────────────────────────
# Профили браузера (куки GitHub/tabitoken, логины, кеш) и живые сессии — НИКОГДА
# не коммитить. tabi-modelmap.json — безопасный конфиг маппинга, коммитим.
tabi/profiles/
tabi/sessions/
tabi/gh-sessions/
routing/tabi-sessions.json

# ── XPeach (вкладка дашборда): приватные данные ───────────────────────────────
# Профили браузера (куки GitHub/xpeach.codes, логины, кеш) и живые сессии —
# НИКОГДА не коммитить. xpeach-modelmap.json — безопасный конфиг маппинга, коммитим.
xpeach/profiles/
xpeach/sessions/
xpeach/gh-sessions/
routing/xpeach-sessions.json

# ── Реф-коды пользователя (💩 в «Настройках» дашборда) ────────────────────────
# Свои коды форка — личная настройка, в репозиторий не уезжают.
# routing/ref-codes.default.json (коды владельца) КОММИТИМ: без него форк регистрировал
# бы аккаунты вообще без рефки.
routing/ref-codes.json

# ── JustWoker (вкладка дашборда): приватные данные ────────────────────────────
# Профили браузера (куки GitHub/api.justwoker.icu, логины, кеш) и живые сессии —
# НИКОГДА не коммитить. justwoker-modelmap.json — безопасный конфиг маппинга, коммитим.
justwoker/profiles/
justwoker/sessions/
justwoker/gh-sessions/
routing/justwoker-sessions.json

# ── SeekAi (вкладка дашборда): приватные данные ───────────────────────────────
# Профили браузера (куки GitHub/seekai.cc, логины, кеш) и живые сессии — НИКОГДА
# не коммитить. seekai-modelmap.json — безопасный конфиг маппинга, коммитим.
seekai/profiles/
seekai/sessions/
seekai/gh-sessions/
routing/seekai-sessions.json

# ── Cookie-jar New-API (routing/lib/newapi-account.js) ────────────────────────
# Живые session/refresh-куки и access-токены аккаунтов agentrouter/gorouter/tabi/
# xpeach/justwoker/seekai, снятые из профилей Chromium. Refresh-кука одноразовая, поэтому её
# ротированное значение хранится здесь, а не в профиле. НИКОГДА не коммитить.
routing/newapi-jar.json

# ── GitHub-аккаунты (вкладка дашборда): приватные данные ──────────────────────
# Профили браузера (куки/сессии GitHub на аккаунт) — НИКОГДА не коммитить.
# github-accounts.json уже закрыт выше, github-accounts.example.json коммитим.
github/profiles/

# Снимки GitHub-сессий (storageState: cookies user_session и компания), которые
# заселяются в профили новых аккаунтов New-API-вкладок. Живой доступ к аккаунтам —
# НИКОГДА не коммитить. Кладёт их github/harvest-session.js, читает
# routing/lib/github-session.js.
github/sessions/
# Суточная история финансов (пишется чеком баланса, у каждого своя)
routing/finance-history.jsonl

# Бэкапы пулов от «🔗 Сверить привязки» (POST /api/gh/relink): копия пула ПЕРЕД
# дописыванием ghId. Внутри живые api_key — в репо им не место, как и самим пулам.
*.relink.bak

# ── Бэкапы разметки дашборда перед вклейкой вкладок ───────────────────────────
# Их делает инжектор (proxy-dashboard.bak-before-*.html) — это снимок на 18 тыс.
# строк, в репозитории он мусор. Однажды уехал туда `git add -A` (23.08).
routing/*.bak-*.html
routing/*.bak

# точки возврата snapshot-rollback.sh (локальные копии, не в git)


# рантайм вотчдога пулов (генерируется)
routing/pool-alert.json
routing/pool-watchdog.log

# точки возврата rescue.js (локальные копии состояния, содержат ключи)
routing/.rescue/

# ── TrueSota (вкладка дашборда): приватные данные ─────────────────────────────
# Провайдер заводится параллельно, и его папки приехали в дерево нетрекаемыми —
# ровно как seekai/ 25.08, когда `git add -A` опубликовал бы живые куки GitHub.
# Пока они пусты, и закрыть их надо ДО первого прогона сессии, а не после.
# truesota-modelmap.json — безопасный конфиг маппинга, его коммитим.
truesota/profiles/
truesota/sessions/
truesota/gh-sessions/
routing/truesota-sessions.json

# ── KKtoken (вкладка дашборда): приватные данные ──────────────────────────────
# Восьмой шлюз, заводится 31.08. Папки закрываем ДО первого прогона сессии: там
# профили браузера и ЖИВЫЕ куки GitHub (kktoken/gh-sessions/acct_*.json).
# kktoken-modelmap.json — безопасный конфиг маппинга, его коммитим.
kktoken/profiles/
kktoken/sessions/
kktoken/gh-sessions/
routing/kktoken-sessions.json

# ── HCNsec (вкладка дашборда): приватные данные ────────────────────────────────
# Девятый шлюз, заводится 31.08. Закрываем ДО первого прогона сессии, а не после:
# 31.08 живые куки двух шлюзов уже лежали в дереве нетрекаемыми, и один `git add -A`
# опубликовал бы их в публичный репозиторий.
# Папки gh-sessions тут нет намеренно: GitHub-OAuth у hcnsec отключён
# (`/api/status`: github_oauth=false), вход только email+пароль — снимать нечего.
# hcnsec-modelmap.json — безопасный конфиг маппинга, его коммитим.
hcnsec/profiles/
hcnsec/sessions/
routing/hcnsec-sessions.json

# ── WisdomSatan (вкладка дашборда): приватные данные ──────────────────────────
# Десятый шлюз, заводится 2026-09-10. Закрываем ДО первого прогона сессии — правило
# выучено 31.08, когда живые куки двух шлюзов лежали в дереве нетрекаемыми и один
# `git add -A` опубликовал бы их в публичный репозиторий.
# В пуле лежат ПАРОЛИ от аккаунтов панели: вход тут только логин+пароль, и ЛК —
# единственный источник точного остатка, так что пароль хранить приходится.
# Папки gh-sessions тут нет намеренно, как и у hcnsec: у `api.wisdomsatan.club`
# `/api/status` отдаёт github_oauth=false (ни GitHub, ни Telegram, ни WeChat, ни
# OIDC — только пароль и passkey), снимать нечего.
# wisdomsatan-modelmap.json — безопасный конфиг маппинга, его коммитим.
wisdomsatan/profiles/
wisdomsatan/sessions/
routing/wisdomsatan-sessions.json

# ── Менеджер Outlook-ящиков (2026-08-31) ─────────────────────────────────────
# В пуле живые пароли от почт, в профилях — куки залогиненных ящиков. Закрыто ДО
# первого импорта: 31.08 папки двух шлюзов уже лежали в дереве нетрекаемыми, и один
# `git add -A` опубликовал бы их в публичный репозиторий.
# accounts.example.json — коммитим: на чистой машине иначе непонятно, что кладём.
outlook/accounts.json
outlook/accounts.json.tmp
outlook/profiles/
outlook/sessions/

# Отстойник восстановления из транскриптов (05.09) — рабочий мусор, не код репо.
.recovered/

# Лига: конфиг отправителя содержит секрет приёмника, данные приёмника — чужие срезы
routing/league-config.json
routing/league-data/
routing/league-peers.json
routing/league-e2e-key.json
routing/archive/

# AIPM
aipm/profiles/
aipm/sessions/
aipm/gh-sessions/
routing/aipm-sessions.json

# MEDIA (vkladka generacii) - vsyo nizhe generiruetsya i v publichnyy repo ne edet.
# Video i webp obshchih pravil ne imeyut: *.png i *.jpg est, *.mp4/*.webm/*.gif/*.webp - net.
routing/media-out/
routing/media-history.jsonl
routing/media-models-cache.json
routing/media-profiles.json
